test -f ~/.bash/paths      && . ~/.bash/paths      # idem for bash and zsh
test -f ~/.zsh/config      && . ~/.zsh/config
test -f ~/.bash/aliases    && . ~/.bash/aliases    # idem for bash and zsh
test -f ~/.zsh/completion  && . ~/.zsh/completion
test -f ~/.rvm/scripts/rvm && . ~/.rvm/scripts/rvm